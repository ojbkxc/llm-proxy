## 2026-09-04 trae_proxy.py 进度盘点
- 凭证解密层实测通过（py311 + cryptography 50）：storage.json 信封解密成功，token 有效期 2026-09-16 18:18，信封 host 为 https://api.trae.cn。
- 代码主体已完成（三协议转换 + SSE 状态机 + tool_call→interrupt），但**从未运行过**（无 trae_proxy.log）。
- 最大缺口：create_agent_task / workflow/start 请求体是占位模板（真实 ~88KB），未验证上游是否接受。
- 代码引用的 trae_solo_analysis/ 与 trae_mitm/ 目录在工作区不存在。
- 待办：mitm 抓真实报文替换 build_create_body/build_workflow_body → 起服务验证 /health + hello 请求。

## 2026-09-04 晚间：trae_proxy 上游实测迭代（trae_probe.py，30+ case）
- **create_agent_task 已打通过两次**（21:26-21:29，经本地服务全链路）。第二代请求体确认正确：user_input 必须是 {id, type, data:{content}} 对象（缺 id 报 binding 错）、model_name 与 custom_model.model_name 都用 __dev 后缀、config_name 用官方名。
- 成功后错误推进到 workflow/start 缺 workflow_info（WorkflowInfo 三字段：workflow_name/workflow_id/node_name，DLL 已确认，待验证取值）。
- **summary 错误是有状态的**：请求逐字节等价时成功/失败并存（dump 对比证实），成功窗口外连发 8 次全失败。主假说：服务端按用户缓存 summary config（短 TTL），由真实 TRAE 客户端活动触发。轮询+用户对照实验进行中。
- 已实测无效字段（勿重复）：scene_location 0/1/2、session_type、chat_mode/mode_type/trae_request_type、use_fast_request、workspace_folders(C:\GitHub)、版本 0.1.60→0.1.62×4 组 version-code、5 个模型。
- llm_utils_chat / llm_raw_chat (api/ide/v1|v2)：可过绑定但模型解析永远 "model is unknown"，判定为服务端固定模型非开放接口，已放弃。
- 客户端实测信息：版本 0.1.62、默认工作区 C:\GitHub、AppId 未变、ai-agent sidecar 端口 40005 按需拉起、ModularData/ai-agent/database.db 加密、模型实际 16 个（代码 MODELS 少 7 个：Doubao-Seed-Evolving/Code、kimi-k3、kimi-k2.7-code、minimax-m3、qwen3.8-max、qwen-3.7-plus）。
- 产出：trae_probe.py（case 即实验记录）、trae_proxy_进展.md（完整结论）、trae_proxy.py gen-2 模板 + TRAE_DUMP 转储开关、WORKSPACE 修正。

## 22:04-22:10 最终突破：SOLO 通道全链路打通 ✅
- 用户引入 traework2api-main（Go 逆向 SOLO 免费 API）+ trae-db-decrypt-master（TRAE CN DB 解密）。
- 关键协议：`llm_utils_chat` + `function=solo_work_lite`，近 OpenAI body，无需 create_agent_task。
- trae_proxy.py 已改造完成：solo_turn_chunks 上游通道 + 动态模型表 + 三协议入口全部实测通过。
- create_agent_task 的 summary 卡点成为历史问题（那条链路不再使用）。
- 备份代码保留：agent_turn_chunks / build_create_body（agent 编排通道）。
